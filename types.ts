//Made by Tommy Ho
//Created a type to store information regarding each post being added to keep track of states
export type PostProps = {
  id: string;
  task: string;
  isfinished: boolean;
  deadline: string;
};
